---
title: NavTab
section: components
---


# NavTab
---

The NavTop component is a versatile navigation bar designed for use at the top of web pages or applications.


## Props

| Name           | Type                                                                                                                                                                                                           | Default value    | Description                                                                        |
| -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ---------------------------------------------------------------------------------- |
| `left`         | **([LinkElement](#LinkElement) \| [ButtonElement](#ButtonElement) \| [DropdownElement](#DropdownElement) \| [IconElement](#IconElement) \| [CustomElement](#CustomElement) \| [LogoElement](#LogoElement))[]** | -                | Defines the components on the left side of the navigation bar                      |
| `right`        | **([LinkElement](#LinkElement) \| [ButtonElement](#ButtonElement) \| [DropdownElement](#DropdownElement) \| [IconElement](#IconElement) \| [CustomElement](#CustomElement) \| [LogoElement](#LogoElement))[]** | -                | Defines the components on the right side of the navigation bar                     |
| `center`       | **([LinkElement](#LinkElement) \| [ButtonElement](#ButtonElement) \| [DropdownElement](#DropdownElement) \| [IconElement](#IconElement) \| [CustomElement](#CustomElement) \| [LogoElement](#LogoElement))[]** | -                | Defines the components on the center of the navigation bar                         |
| `flexCenter`   | **([LinkElement](#LinkElement) \| [ButtonElement](#ButtonElement) \| [DropdownElement](#DropdownElement) \| [IconElement](#IconElement) \| [CustomElement](#CustomElement) \| [LogoElement](#LogoElement))[]** | -                | Defines the components of the navigation bar                                       |
| `top`          | **[TopProp](#TopProp)[]**                                                                                                                                                                                      | -                | `[Deprecated]` Defines the Nav Top links                                           |
| `blue`         | **boolean**                                                                                                                                                                                                    | -                | Sets blue theme                                                                    |
| `fixed`        | **boolean**                                                                                                                                                                                                    | -                | Sets the NavTab to a fixed position at the top                                     |
| `hideOnScroll` | **boolean**                                                                                                                                                                                                    | -                | Hides the nav on scroll                                                            |
| `bottom`       | **boolean**                                                                                                                                                                                                    | -                | Fixes the NavTab to the bottom of the screen (you have to also set fixed as true). |
| `zIndex`       | **number**                                                                                                                                                                                                     | 999              | Specifies the nav z-index (defaults to 999)                                        |
| `className`    | **string**                                                                                                                                                                                                     | -                | Adds classes to nav container                                                      |
| `isFluid`      | **boolean**                                                                                                                                                                                                    | -                | `[Deprecated]` Adds a simple responsive padding                                      |
| `isResponsive` | **boolean**                                                                                                                                                                                                    | -                | Adds a responsive padding                                                          |


### LinkElement

An object with the `type` property set to 'link', where you can use all **[NavItem props](/NavItem)** except for `children`, which should be replaced with `text`. 
Additional Button props are detailed in the table below.

| Name           | Type                                        | Default value    | Description                             |
| -------------- | ------------------------------------------- | ---------------- | --------------------------------------- |
| `type`         | **link**                                    | -                | Specifies the type of component to use  |
| `key`          | **string \| number**                        | -                | Key of the component                    |
| `text`         | **string \| ReactNode**                     | -                | Defines the child component             |

### ButtonElement

An object with the `type` property set to 'Button', where you can use all **[Button props](/Button)** except for `children`, which should be replaced with `text` and white. 
Additional Button props are detailed in the table below.

| Name           | Type                                        | Default value    | Description                             |
| -------------- | ------------------------------------------- | ---------------- | --------------------------------------- |
| `type`         | **button**                                  | -                | Specifies the type of component to use  |
| `key`          | **string \| number**                        | -                | Key of the component                    |
| `text`         | **string \| ReactNode**                     | -                | Defines the child component             |


### DropdownElement

An object with the `type` property set to 'link', where you can use all **[NavItem props](/NavItem)** except for `children`, which should be replaced with `text` and `selected`.
Additional Button props are detailed in the table below.

| Name           | Type                                        | Default value    | Description                             |
| -------------- | ------------------------------------------- | ---------------- | --------------------------------------- |
| `type`         | **dropdownLink**                            | -                | Specifies the type of component to use  |
| `key`          | **string \| number**                        | -                | Key of the component                    |
| `text`         | **string \| ReactNode**                     | -                | Defines the child component             |

### NavButton

An object with the `type` property set to 'navButton', where you can use all **[NavButton props](/NavButton)**.
Additional NavIcon props are detailed in the table below.

| Name           | Type                                        | Default value    | Description                             |
| -------------- | ------------------------------------------- | ---------------- | --------------------------------------- |
| `type`         | **navButton**                               | -                | Specifies the type of component to use  |
| `key`          | **string \| number**                        | -                | Key of the component                    |


### CustomElement

An object with the `type` property set to 'custom'.

| Name           | Type                                        | Default value    | Description                             |
| -------------- | ------------------------------------------- | ---------------- | --------------------------------------- |
| `type`         | **custom**                                  | -                | Specifies the type of component to use  |
| `key`          | **string \| number**                        | -                | Key of the component                    |
| `custom`       | **ReactNode**                               | -                | Defines the child component             |
| `testId`       | **string**                                  | -                | Anchor test id                          |

### LogoElement 

An object with the `type` property set to 'logo'.

| Name           | Type                                        | Default value    | Description                             |
| -------------- | ------------------------------------------- | ---------------- | --------------------------------------- |
| `type`         | **logo**                                    | -                | Specifies the type of component to use  |
| `key`          | **string \| number**                        | -                | Key of the component                    |
| `logo`         | **ReactNode**                               | -                | Defines the child component             |
| `testId`       | **string**                                  | -                | Anchor test id                          |

### TopProp
An object where you can use all **[NavItem props](/NavItem)** except for `white`, `small` and `className`.

## Examples

### NavTab Unlogged

<Demo title="NavTab Unlogged" background={colors.white}>
  <Function>
    { () => {
      const [selected, setSelected] = useState('Vacantes');
      const left = [
        {
          key: 0,
          type: 'logo',
          logo: (
            <a href="#NavTab" style={{ display: 'flex' }}>
              <Logo variant="occLogo" theme="white" />
            </a>
          )
        },
        {
          key: 1,
          type: 'navButton',
          label: 'Vacantes',
          className: 'only-desktop',
          selected: selected === 'Vacantes',
          showBar: selected === 'Vacantes',
          onClick: () => setSelected('Vacantes')
        },
        {
          key: 2,
          type: 'navButton',
          label: 'Talento',
          className: 'only-desktop',
          selected: selected === 'Talento',
          showBar: selected === 'Talento',
          onClick: () => setSelected('Talento')
        },
        {
          key: 3,
          type: 'navButton',
          label: 'Ayuda',
          className: 'only-desktop',
          selected: selected === 'Ayuda',
          showBar: selected === 'Ayuda',
          onClick: () => setSelected('Ayuda')
        }
      ];
      const left2 = [...left];
      left2[0] = {
        key: 0,
        type: 'logo',
        logo: (
          <a href="#NavTab" style={{ display: 'flex' }}>
            <Logo variant="occLogo" />
          </a>
        )
      };
      const right = [
        {
          key: 0,
          type: 'button',
          text: 'Juan Alvez',
          size: 'md',
          className: 'only-desktop',
        },
        {
          key: 1,
          type: 'button',
          text: 'Juan Alvez',
          size: 'md',
          theme: 'tertiaryWhite',
          className: 'only-desktop',
        },
        {
          key: 2,
          type: 'button',
          iconLeft: 'search'
        },
        {
          key: 3,
          type: 'button',
          className: 'only-mobile',
          iconLeft: 'menu'
        },
      ];
      const right2 = [...right];
      right2[0] = {
        ...right[0],
        theme: 'ghostGrey'
      };
      right2[1] = {
        ...right[1],
        theme: 'tertiary'
      };
      return (
        <>
          <style>
            {`
              @media screen and (min-width: ${grid.sm}px) {
                .only-mobile {
                  display: none;
                } 
              }
              @media screen and (max-width: ${grid.sm - 1}px) {
                .only-desktop {
                  display: none;
                } 
              }
            `}
          </style>
          <p>
            In this exaple isFluid (deprecated property) is used instead of isResponsise (preferable) to avoid to break the example.
          </p>
          <br/>
          <NavTab isFluid blue left={left} right={right} />
          <NavTab isFluid left={left2} right={right2} />
        </>
      );
    }}
  </Function>
</Demo>

```jsx
// Example
function Component() {
  const [selected, setSelected] = useState('Vacantes');
  const left = [
    {
      key: 0,
      type: 'logo',
      logo: (
        <a href="#NavTab" style={{ display: 'flex' }}>
          <Logo variant="occLogo" theme="white" />
        </a>
      )
    },
    {
      key: 1,
      type: 'navButton',
      label: 'Vacantes',
      className: 'only-desktop',
      selected: selected === 'Vacantes',
      showBar: selected === 'Vacantes',
      onClick: () => setSelected('Vacantes')
    },
    {
      key: 2,
      type: 'navButton',
      label: 'Talento',
      className: 'only-desktop',
      selected: selected === 'Talento',
      showBar: selected === 'Talento',
      onClick: () => setSelected('Talento')
    },
    {
      key: 3,
      type: 'navButton',
      label: 'Ayuda',
      className: 'only-desktop',
      selected: selected === 'Ayuda',
      showBar: selected === 'Ayuda',
      onClick: () => setSelected('Ayuda')
    }
  ];
  const left2 = [...left];
  left2[0] = {
    key: 0,
    type: 'logo',
    logo: (
      <a href="#NavTab" style={{ display: 'flex' }}>
        <Logo variant="occLogo" />
      </a>
    )
  };
  const right = [
    {
      key: 0,
      type: 'button',
      text: 'Juan Alvez',
      size: 'md',
      className: 'only-desktop',
    },
    {
      key: 1,
      type: 'button',
      text: 'Juan Alvez',
      size: 'md',
      theme: 'tertiaryWhite',
      className: 'only-desktop',
    },
    {
      key: 2,
      type: 'button',
      iconLeft: 'search'
    },
    {
      key: 3,
      type: 'button',
      className: 'only-mobile',
      iconLeft: 'menu'
    },
  ];
  const right2 = [...right];
  right2[0] = {
    ...right[0],
    theme: 'ghostGrey'
  };
  right2[1] = {
    ...right[1],
    theme: 'tertiary'
  };
  return (
    <>
      <style>
        {`
          @media screen and (min-width: ${grid.sm}px) {
            .only-mobile {
              display: none;
            } 
          }
          @media screen and (max-width: ${grid.sm - 1}px) {
            .only-desktop {
              display: none;
            } 
          }
        `}
      </style>
      <p>
        In this exaple isFluid (deprecated property) is used instead of isResponsise (preferable) to avoid to break the example.
      </p>
      <br/>
      <NavTab isFluid blue left={left} right={right} />
      <NavTab isFluid left={left2} right={right2} />
    </>
  );
}
```

### NavTab Logged

<Demo title="NavTab Logged" background={colors.white}>
  <Function>
    { () => {
      const [selected, setSelected] = useState('Vacantes');
      const left = [
        {
          key: 0,
          type: 'logo',
          logo: (
            <a href="#NavTab" style={{ display: 'flex' }}>
              <Logo variant="occLogo" theme="white" />
            </a>
          )
        },
        {
          key: 1,
          type: 'navButton',
          label: 'Vacantes',
          className: 'only-desktop',
          selected: selected === 'Vacantes',
          showBar: selected === 'Vacantes',
          onClick: () => setSelected('Vacantes')
        },
        {
          key: 2,
          type: 'navButton',
          label: 'Talento',
          className: 'only-desktop',
          selected: selected === 'Talento',
          showBar: selected === 'Talento',
          onClick: () => setSelected('Talento')
        },
        {
          key: 3,
          type: 'navButton',
          label: 'Ayuda',
          className: 'only-desktop',
          selected: selected === 'Ayuda',
          showBar: selected === 'Ayuda',
          onClick: () => setSelected('Ayuda')
        }
      ];
      const left2 = [...left];
      left2[0] = {
        key: 0,
        type: 'logo',
        logo: (
          <a href="#NavTab" style={{ display: 'flex' }}>
            <Logo variant="occLogo" />
          </a>
        )
      };
      const right = [
        {
          key: 0,
          type: 'button',
          className: 'only-desktop',
          iconLeft: 'search'
        },
        {
          key: 1,
          type: 'custom',
          custom: <MenuDropdown className={{ menu: "menu", button: "only-desktop" }} darkMode />
        },
        {
          key: 2,
          type: 'button',
          className: 'only-mobile',
          iconLeft: 'menu'
        },
      ];
      const right2 = [...right];
      right2[1] = {
        ...right[1],
        custom: <MenuDropdown className={{ menu: "menu", button: "only-desktop" }}  />
      };
      return (
        <>
          <style>
            {`
              @media screen and (min-width: ${grid.sm}px) {
                .only-mobile {
                  display: none;
                } 
              }
              @media screen and (max-width: ${grid.sm - 1}px) {
                .only-desktop {
                  display: none;
                } 
              }
              .menu {
                z-index: 1000;
              }
            `}
          </style>
          <p>
            In this exaple isFluid (deprecated property) is used instead of isResponsise (preferable) to avoid to break the example.
          </p>
          <br/>
          <NavTab isFluid blue left={left} right={right} />
          <NavTab isFluid left={left2} right={right2} />
        </>
      );
    }}
  </Function>
</Demo>

```jsx  
// Example
function Component() {
  const [selected, setSelected] = useState('Vacantes');
  const left = [
    {
      key: 0,
      type: 'logo',
      logo: (
        <a href="#NavTab" style={{ display: 'flex' }}>
          <Logo variant="occLogo" theme="white" />
        </a>
      )
    },
    {
      key: 1,
      type: 'navButton',
      label: 'Vacantes',
      className: 'only-desktop',
      selected: selected === 'Vacantes',
      showBar: selected === 'Vacantes',
      onClick: () => setSelected('Vacantes')
    },
    {
      key: 2,
      type: 'navButton',
      label: 'Talento',
      className: 'only-desktop',
      selected: selected === 'Talento',
      showBar: selected === 'Talento',
      onClick: () => setSelected('Talento')
    },
    {
      key: 3,
      type: 'navButton',
      label: 'Ayuda',
      className: 'only-desktop',
      selected: selected === 'Ayuda',
      showBar: selected === 'Ayuda',
      onClick: () => setSelected('Ayuda')
    }
  ];
  const left2 = [...left];
  left2[0] = {
    key: 0,
    type: 'logo',
    logo: (
      <a href="#NavTab" style={{ display: 'flex' }}>
        <Logo variant="occLogo" />
      </a>
    )
  };
  const right = [
    {
      key: 0,
      type: 'button',
      className: 'only-desktop',
      iconLeft: 'search'
    },
    {
      key: 1,
      type: 'custom',
      custom: <MenuDropdown className={{ menu: "menu", button: "only-desktop" }} darkMode />
    },
    {
      key: 2,
      type: 'button',
      className: 'only-mobile',
      iconLeft: 'menu'
    },
  ];
  const right2 = [...right];
  right2[0] = {
    ...right[0],
    custom: <MenuDropdown className={{ menu: "menu", button: "only-desktop" }} />
  };
  return (
    <>
      <style>
        {`
          @media screen and (min-width: ${grid.sm}px) {
            .only-mobile {
              display: none;
            } 
          }
          @media screen and (max-width: ${grid.sm - 1}px) {
            .only-desktop {
              display: none;
            } 
          }
          .menu {
            zIndex: 1000
          }
        `}
      </style>
      <p>
        In this exaple isFluid (deprecated property) is used instead of isResponsise (preferable) to avoid to break the example.
      </p>
      <br/>
      <NavTab isFluid blue left={left} right={right} />
      <NavTab isFluid left={left2} right={right2} />
    </>
  );
}
```

### NavTab with center

<Demo title="NavTab with center" background={colors.white}>
  <Function>
    { () => {
      const [dropdown, setDropdown] = useState(false)
      const center = [{
        key: 1,
        type: 'navButton',
        iconName: 'columns-gap'
      },
      {
        key: 2,
        type: 'navButton',
        iconName: 'search'
      },
      {
        key: 3,
        type: 'navButton',
        iconName: 'person'
      }]
      const left = [{
        key: 1,
        type: 'button',
        iconLeft: 'menu'
      }]
      const right = [{
        key: 1,
        type: 'button',
        iconLeft: 'chat'
      }]
      return (
        <>
          <NavTab isFluid blue center={center} left={left} right={right} />
          <NavTab isFluid center={center} left={left} right={right} />
        </>
      )
    }}
  </Function>
</Demo>

```jsx
// How to use NavTab with center
function Component() {
  const [dropdown, setDropdown] = useState(false)
  const center = [{
    key:1,
    type:'navButton',
    iconName:'columns-gap'
  },
  {
    key:2,
    type:'navButton',
    iconName:'search'
  },
  {
    key:3,
    type:'navButton',
    iconName:'person'
  }]
  const left = [{
    key:1,
    type: 'navButton',
    iconName: 'menu'
  }]
  const right = [{
    key:1,
    type:'navButton',
    iconName:'chat'
  }]
  return (
    <>
      <NavTab isFluid blue center={center} left={left} right={right} />
      <NavTab isFluid center={center} left={left} right={right} />
    </>
  )
}
```

### NavTab with NavIcons switching

<Demo title="NavTab with NavIcons switching" background={colors.white}>
  <Function>
    { () => {
      const [selected, setSelected] = useState('')
      const center = [{
        key:1,
        type:'navButton',
        iconName: 'columns-gap',
        label: 'Dashboard',
        selected: selected === 'columns-gap',
        showBar: selected === 'columns-gap',
        onClick: () => setSelected('columns-gap')
      },
      {
        key:2,
        type:'navButton',
        iconName:'search',
        label: 'Search',
        selected: selected === 'search',
        showBar: selected === 'search',
        onClick: () => setSelected('search')
      },
      {
        key:3,
        type:'navButton',
        iconName:'person',
        label: 'Profile',
        selected: selected === 'person',
        showBar: selected === 'person',
        onClick: () => setSelected('person')
      }]
      const left = [{
        key:1,
        type: 'logo',
        logo: (
          <a href="#NavTab" style={{ display: 'flex' }}>
            <Logo variant="occLogo" theme="white" />
          </a>
        )
      }]
      const left2 = [{
        key:1,
        type: 'logo',
        logo: (
          <a href="#NavTab" style={{ display: 'flex' }}>
            <Logo variant="occLogo" theme="blue" />
          </a>
        )
      }]
      const right = [{
        key:1,
        type:'button',
        iconLeft:'cart'
      },
      {
        key:2,
        type:'button',
        iconLeft:'chat'
      },
      {
        key: 3,
        type: 'button',
        text: 'Juan Alvez',
        size: 'md',
        theme: 'tertiaryWhite'
      }]
      const right2 = [...right]
      right2[2] = {
        ...right[2],
        theme: 'tertiary'
      }
      return (
        <>
          <NavTab isFluid blue center={center} left={left} right={right} />
          <NavTab isFluid center={center} left={left2} right={right2} />
        </>
      )
    }}
  </Function>
</Demo>

```jsx
// How to use NavTab with NavIcons switching
function Component() {
  const [selected, setSelected] = useState('')
  const center = [{
    key:1,
    type:'navButton',
    iconName: 'columns-gap',
    label: 'Dashboard',
    selected: selected === 'columns-gap',
    showBar: selected === 'columns-gap',
    onClick: () => setSelected('columns-gap')
  },
  {
    key:2,
    type:'navButton',
    iconName:'search',
    label: 'Search',
    selected: selected === 'search',
    showBar: selected === 'search',
    onClick: () => setSelected('search')
  },
  {
    key:3,
    type:'navButton',
    iconName:'person',
    label: 'Profile',
    selected: selected === 'person',
    showBar: selected === 'person',
    onClick: () => setSelected('person')
  }]
  const left = [{
    key:1,
    type: 'logo',
    logo: (
      <a href="#NavTab" style={{ display: 'flex' }}>
        <Logo variant="occLogo" theme="white" />
      </a>
    )
  }]
  const left2 = [{
    key:1,
    type: 'logo',
    logo: (
      <a href="#NavTab" style={{ display: 'flex' }}>
        <Logo variant="occLogo" theme="blue" />
      </a>
    )
  }]
  const right = [{
    key:1,
    type:'navButton',
    iconName:'cart',
    selected: selected === 'cart',
    showBar: selected === 'cart',
    onClick: () => setSelected('cart')
  },
  {
    key:2,
    type:'navButton',
    iconName:'chat',
    selected: selected === 'chat',
    showBar: selected === 'chat',
    onClick: () => setSelected('chat')
  },
  {
    key: 3,
    type: 'button',
    text: 'Juan Alvez',
    size: 'md',
    theme: 'tertiaryWhite'
  }]
  return (
    <>
      <NavTab isFluid blue center={center} left={left} right={right} />
      <NavTab isFluid center={center} left={left2} right={right} />
    </>
  )
}
```

### NavTab with icons

<Demo title="NavTab with icons" background={colors.white}>
  <Function>
    {() => {
      const flexCenter = [{
        key:1,
        type:'navButton',
        iconName:'columns-gap',
        label: 'Dashboard',
        selected: false,
        onClick: () => { console.log('dashboard') },
        direction: 'col',
        width: 96
      },
      {
        key:2,
        type:'navButton',
        iconName:'search',
        label: 'Search',
        selected: false,
        onClick: () => { console.log('search') },
        direction: 'col',
        width: 96
      },
      {
        key:3,
        type:'navButton',
        iconName:'person',
        label: 'Profile',
        selected: true,
        onClick: () => { console.log('profile') },
        direction: 'col',
        width: 96
      }]
      return (
        <>
          <NavTab isFluid blue flexCenter={flexCenter} />
          <NavTab isFluid flexCenter={flexCenter} />
        </>
      )
    }}
  </Function>
</Demo>

```jsx
// How to use NavTab with icons
function Component() {
  const flexCenter = [{
    key:1,
    type:'navButton',
    iconName:'columns-gap',
    label: 'Dashboard',
    selected: false,
    onClick: () => { console.log('dashboard') },
    direction: 'col',
    width: 96
  },
  {
    key:2,
    type:'navButton',
    iconName:'search',
    label: 'Search',
    selected: false,
    onClick: () => { console.log('search') },
    direction: 'col',
    width: 96
  },
  {
    key:3,
    type:'navButton',
    iconName:'person',
    label: 'Profile',
    selected: true,
    onClick: () => { console.log('profile') },
    direction: 'col',
    width: 96
  }]
  return (
    <>
      <NavTab isFluid blue flexCenter={flexCenter} />
      <NavTab isFluid flexCenter={flexCenter} />
    </>
  )
}
```

### NavTab with NavTop

<Demo title="[Deprecated] NavTab with NavTop" background={colors.white}>
  <Function>
    { () => {
      const [dropdown, setDropdown] = useState(false)
      const top = [
        {
          key:1,
          children:'First link',
          link:'#NavTab'
        },
        {
          key:2,
          children:'Current link',
          link:'#NavTab',
          selected: true
        },
        {
          key:3,
          children:'onClick item',
          onClick:() => { console.log("onClick item") }
        }
      ]
      const left = [
        {
            key:0,
            type: 'logo',
            logo: (
              <a href="#NavTab" style={{ display: 'flex' }}>
                <Logo variant="occLogo" theme="white" />
              </a>
            )
        },
        {
          key: 1,
          type: 'dropdownLink',
          text: 'Dropdown link',
          onClick: () => setDropdown(!dropdown),
          selected: dropdown
        },
        {
          key: 2,
          type: 'link',
          text: 'Standard link',
          link: '#NavTab'
        }
      ]
      const left2 = [...left]
      left2[0] = {
        key:0,
        type: 'logo',
        logo: (
          <a href="#NavTab">
            <Logo variant="occLogo" theme="blue" />
          </a>
        )
      }
      const right = [
        {
          key:1,
          type:'button',
          text:'Primary CTA',
          theme:'primary'
        },
        {
          key:2,
          type:'button',  
          text:'Secondary CTA',
          theme:'ghostWhite'
        }
      ]
      const right2 = [...right]
      right2[1].theme = 'ghost'
      return (
        <>
          <NavTab isFluid blue top={top} left={left} right={right} />
          <NavTab isFluid top={top} left={left2} right={right2} />
        </>
      )
    }}
  </Function>
</Demo>

```jsx
// Example
function Component() {
  const [dropdown, setDropdown] = useState(false)
  const top = [
    {
      key: 1,
      children: 'First link',
      link:'#NavTab'
    },
    {
      key: 2,
      children: 'Current link',
      link:'#NavTab',
      selected: true
    },
    {
      key:3,
      children:'onClick item',
      onClick:() => { console.log("onClick item") }
    }
  ]
  const left = [
    {
        key:0,
        type: 'logo',
        logo: (
          <a href="#NavTab" style={{ display: 'flex' }}>
            <Logo variant="occLogo" theme="white" />
          </a>
        )
    },
    {
      key: 1,
      type: 'dropdownLink',
      text: 'Dropdown link',
      onClick: () => setDropdown(!dropdown),
      selected: dropdown
    },
    {
      key: 2,
      type: 'link',
      text: 'Standard link',
      link: '#NavTab'
    }
  ]
  const left2 = [...left]
  left2[0] = {
    key: 0,
    type: 'logo',
    logo: (
      <a href="#NavTab">
        <Logo variant="occLogo" theme="blue" />
      </a>
    )
  }
  const right = [
    {
      key: 1,
      type: 'button',
      text: 'Primary CTA',
      theme: 'primary'
    },
    {
      key: 2,
      type: 'button',  
      text: 'Secondary CTA',
      theme: 'ghostWhite'
    }
    const right2 = [...right]
    right2[1].theme = 'ghost'
  ]
  return (
    <>
      <NavTab isFluid blue top={top} left={left} right={right} />
      <NavTab isFluid top={top} left={left2} right={right} />
    </>
  )
}
```

### NavTab with center

<Demo title="NavTab with center" background={colors.white}>
  <Function>
    { () => {
      const [dropdown, setDropdown] = useState(false)
      const center = [{
        key: 1,
        type: 'navButton',
        iconName: 'columns-gap'
      },
      {
        key: 2,
        type: 'navButton',
        iconName: 'search'
      },
      {
        key: 3,
        type: 'navButton',
        iconName: 'person'
      }]
      const left = [{
        key: 1,
        type: 'button',
        iconLeft: 'menu'
      }]
      const right = [{
        key: 1,
        type: 'button',
        iconLeft: 'chat'
      }]
      return (
        <>
          <NavTab isFluid blue center={center} left={left} right={right} />
          <NavTab isFluid center={center} left={left} right={right} />
        </>
      )
    }}
  </Function>
</Demo>

```jsx
// How to use NavTab with center
function Component() {
  const [dropdown, setDropdown] = useState(false)
  const center = [{
    key: 1,
    type: 'navButton',
    iconName: 'columns-gap'
  },
  {
    key: 2,
    type: 'navButton',
    iconName: 'search'
  },
  {
    key: 3,
    type: 'navButton',
    iconName: 'person'
  }]
  const left = [{
    key: 1,
    type: 'button',
    iconLeft: 'menu'
  }]
  const right = [{
    key: 1,
    type: 'button',
    iconLeft: 'chat'
  }]
  return (
    <>
      <NavTab isFluid blue center={center} left={left} right={right} />
      <NavTab isFluid center={center} left={left} right={right} />
    </>
  )
}
```

### NavTab with NavButtons switching

<Demo title="NavTab with NavButtons switching" background={colors.white}>
  <Function>
    { () => {
      const [selected, setSelected] = useState('dashboard')
      const center = [{
        key:1,
        type:'navButton',
        iconName: 'columns-gap',
        label: 'Dashboard',
        selected: selected === 'dashboard',
        showBar: selected === 'dashboard',
        onClick: () => setSelected('dashboard')
      },
      {
        key:2,
        type:'navButton',
        iconName:'search',
        label: 'Search',
        selected: selected === 'search',
        showBar: selected === 'search',
        onClick: () => setSelected('search')
      },
      {
        key:3,
        type:'navButton',
        iconName:'person',
        label: 'Profile',
        selected: selected === 'profile',
        showBar: selected === 'profile',
        onClick: () => setSelected('profile')
      }]
      const left = [{
        key:1,
        type: 'logo',
        logo: (
          <a href="#NavTab" style={{ display: 'flex' }}>
            <Logo variant="occLogo" theme="white" />
          </a>
        )
      }]
      const left2 = [{
        key:1,
        type: 'logo',
        logo: (
          <a href="#NavTab" style={{ display: 'flex' }}>
            <Logo variant="occLogo" theme="blue" />
          </a>
        )
      }]
      const right = [{
        key:1,
        type:'navButton',
        iconName:'cart',
        selected: selected === 'cart',
        showBar: selected === 'cart',
        onClick: () => setSelected('cart')
      },
      {
        key:2,
        type:'navButton',
        iconName:'chat',
        selected: selected === 'chat',
        showBar: selected === 'chat',
        onClick: () => setSelected('chat')
      },
      {
        key: 3,
        type: 'button',
        text: 'Juan Alvez',
        size: 'md',
        theme: 'tertiaryWhite'
      }]
      const right2 = [...right]
      right2[2].theme = 'tertiary'
      return (
        <>
          <NavTab isFluid blue center={center} left={left} right={right} />
          <NavTab isFluid center={center} left={left2} right={right} />
        </>
      )
    }}
  </Function>
</Demo>

```jsx
// How to use NavTab with NavButtons switching
function Component() {
  const [selected, setSelected] = useState('dashboard')
  const center = [{
    key:1,
    type:'navButton',
    iconName: 'columns-gap',
    label: 'Dashboard',
    selected: selected === 'icon',
    showBar: selected === 'icon',
    onClick: () => setSelected('dashboard')
  },
  {
    key:2,
    type:'navButton',
    iconName:'search',
    label: 'Search',
    selected: selected === 'search',
    showBar: selected === 'search',
    onClick: () => setSelected('search')
  },
  {
    key:3,
    type:'navButton',
    iconName:'person',
    label: 'Profile',
    selected: selected === 'person',
    showBar: selected === 'person',
    onClick: () => setSelected('profile')
  }]
  const left = [{
    key:1,
    type: 'logo',
    logo: (
      <a href="#NavTab" style={{ display: 'flex' }}>
        <Logo variant="occLogo" theme="white" />
      </a>
    )
  }]
  const left2 = [{
    key:1,
    type: 'logo',
    logo: (
      <a href="#NavTab" style={{ display: 'flex' }}>
        <Logo variant="occLogo" theme="blue" />
      </a>
    )
  }]
  const right = [{
    key:1,
    type:'navButton',
    iconName:'cart',
    selected: selected === 'cart',
    showBar: selected === 'cart',
    onClick: () => setSelected('cart')
  },
  {
    key:2,
    type:'navButton',
    iconName:'chat',
    selected: selected === 'chat',
    showBar: selected === 'chat',
    onClick: () => setSelected('chat')
  },
  {
    key: 3,
    type: 'button',
    text: 'Juan Alvez',
    size: 'md',
    theme: 'tertiaryWhite'
  }]
  const right2 = [...right]
  right2[2].theme = 'ghost'
  return (
    <>
      <NavTab isFluid blue center={center} left={left} right={right} />
      <NavTab isFluid center={center} left={left2} right={right} />
    </>
  )
}
```
