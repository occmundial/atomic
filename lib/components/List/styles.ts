import { createUseStyles } from 'react-jss'

export default createUseStyles({
  container: {
    marginBlock: 0,
    paddingBlock: 0,
    paddingInline: 0
  },
  paddingVertical: {
    paddingTop: 4,
    paddingBottom: 4
  }
})
